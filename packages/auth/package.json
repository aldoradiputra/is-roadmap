{
  "name": "@kantr/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Auth primitives: password hashing, session tokens, cookie helpers, DB-backed session management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@kantr/db": "workspace:*"
  },
  "peerDependencies": {
    "@kantr/db": "workspace:*"
  }
}
